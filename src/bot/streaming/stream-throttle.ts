// Temporary re-export shim: the module moved to src/utils (platform-neutral).
// Deleted together with the Telegram layer in the port's cleanup phase.
export * from "../../utils/stream-throttle.js";
